using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridTestingScript : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        FloorGrid grid = new FloorGrid(20, 10);
    }

}
